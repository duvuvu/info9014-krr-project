# Per D-05: raw IMDb dump (~1.6 GB compressed) is gitignored.
# Recover via: bash database/etl/download.sh
raw/

# Per D-06: filtered slice may or may not be committed depending on size.
# At default N=2500 the slice is ~25 MB and committable; we keep it tracked
# by default. To untrack, uncomment the next line:
# filtered/

# Working files produced during filtering — never committed
filtered/tconsts.txt
filtered/nconsts.txt
filtered/.n_*
filtered/connectivity_report.txt
